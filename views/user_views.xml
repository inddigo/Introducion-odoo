<odoo>
    <record id="view_users_form_inherit_unidad" model="ir.ui.view">
        <field name="name">res.users.form.inherit.unidad</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="inside">
                <page string="Unidades de Negocio" name="unidades_negocio_page">
                    <group string="Configuración de Acceso">
                        <field name="unidad_ids" widget="many2many_tags" options="{'no_create': True}"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>
</odoo>